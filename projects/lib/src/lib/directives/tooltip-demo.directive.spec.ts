import { TooltipDemoDirective } from './tooltip-demo.directive';

describe('TooltipDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipDemoDirective();
    expect(directive).toBeTruthy();
  });
});
